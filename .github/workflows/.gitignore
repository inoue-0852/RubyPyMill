# RubyGem build artifacts
/pkg/
/coverage/
/.yardoc

# Bundler / dependencies
/vendor/
/.bundle/
# Gemfile.lock はライブラリ(Gem)では除外が推奨、アプリでは追跡可
Gemfile.lock

# Python venv
/.venv/
/__pycache__/
/pycache/

# Editor / system files
*.swp
.DS_Store

# Outputs / notebooks (動的出力は除外、サンプルは保持)
examples/outputs/tmp*/
examples/outputs/*.tmp
